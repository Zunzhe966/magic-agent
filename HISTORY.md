# 版本历史（HISTORY）

> 本文件是**人类可读的版本脉络**，记录每个版本的节点、做了什么、对应哪个 git tag。
> 项目只有一条主线（`main` 分支），历史全部保存在 git 里，**不另设 backup 目录**。
> 权威架构约束见 `CONTRACT.md`；完整提交记录用 `git log`。

## 版本一览

| 版本 | 日期 | git tag | 一句话 |
|------|------|---------|--------|
| v0.0.1 | 2026-08-20 | `v0.0.1` | 首个可运行版本：进程级智能分流 + 云服务器控制台 |
| v0.0.2 | 2026-08-21 | `v0.0.2` | 订阅拉取、冲突检测、Keychain 凭据、节点管理 |
| v0.0.3 | 2026-08-21 | `v0.0.3` | TUN 模式 + 特权 mihomo，进程分流可靠化 |
| v0.0.4 | 2026-08-21 | `v0.0.4` | MCP 服务：AI 可控制魔法代理 |
| v0.0.5 | 2026-08-21 | `v0.0.5` | 域名分流规则 |
| v0.1.0 | 2026-08-22 | `v0.1.0` | 加密 DNS（DoH/DoT）+ 按软件选节点 |
| v0.1.1 | 2026-09-03 | `v0.1.1` | 修复 mihomo 孤儿进程劫持全机流量事故 |
| v0.2.0 | 2026-08-31 | `v0.2.0` | 云服务器管理与双路探测 |
| v0.2.2 | 2026-09-19 | `v0.2.2` | 启动即清理第三方代理 + 卡顿修复 + 规范发版流程 |

> 说明：v0.2.1 只改过内部版本号、未单独发版，故不列。版本号以 `tauri.conf.json` 为准。

---

## 各版本详情

### v0.2.2（2026-09-19）— 当前版本

- **启动即清理第三方代理**：App 启动 800ms 后、以及每次启动代理时，自动关闭系统里其他代理软件（FlClash / Clash Verge / ClashX / V2Ray / Xray / Surge / sing-box 等），并关闭系统代理，让网络回到干净状态，再启动本程序代理。**保证本程序是系统唯一代理。**
- **卡顿修复**：找出 UI 卡顿根因——`scan_apps` 的 `lsof` 全机网络扫描耗时 1–3 秒，且启动时同步等待。改为 lsof 加 2.5 秒硬超时 + 软件扫描后台异步，首屏即时可点。
- **MCP 增强**：`_active_server` 支持从选中代理节点推导 SSH 主机（与后端对齐）；`ssh_exec` 密码分支透传远端退出码。
- **规范发版流程**：确立铁律——必须走 `scripts/release.sh`（同步版本号 + 签名 + 生成 updater-feed），**禁止手工 cp 覆盖**。详见 `CONTRACT.md`。
- 此前多轮积压的修复（SSH 孤儿进程清理、mihomo 看门狗、SSH 与节点融合、若干前端时序 bug）一并随本版入库。

### v0.2.0（2026-08-31）

- 云服务器管理：多服务器保存、切换、删除；Keychain 存密码。
- 双路探测：直连 / 代理两条路径对照，判断节点是否真的生效。
- 全量文档脱敏 + 与实现对齐。
- 更换专属图标（竹子 + 能量球），应用更名为「尊者魔法代理」。

### v0.1.1（2026-09-03）— 事故修复

- **事故**：App 关闭后 root 权限的 mihomo 内核变成孤儿进程，以 TUN 接管全机流量，导致大量应用流量被劫持（ECONNRESET / 502）。
- **修复**：App 增加退出清理钩子（`RunEvent::Exit` 时停内核 + 关系统代理）；root 进程杀不掉时用提权兜底；钉死出口网卡避免误判。铁律：**App 生命周期必须完整覆盖内核生命周期**。

### v0.1.0（2026-08-22）

- 加密 DNS（DoH / DoT）。
- 按软件单独选节点（某软件走 A 节点，另一软件走 B 节点）。
- 新增美国住宅节点。

### v0.0.5（2026-08-21）

- 域名分流规则：按域名决定走直连还是代理（解决同一来源下载时域名混杂的问题）。

### v0.0.4（2026-08-21）

- MCP 服务：让 AI 客户端（智能体）能直接控制代理——查状态、切节点、测延迟、执行 SSH 命令等。

### v0.0.3（2026-08-21）

- TUN 模式 + 特权 mihomo：进程级分流更可靠（不再依赖应用主动走系统代理）。
- 生成配置用 `mihomo -t` 真校验。

### v0.0.2（2026-08-21）

- 订阅拉取：支持订阅链接导入节点。
- 冲突检测：启动前检测端口占用 / 第三方代理。
- Keychain 保存 SSH 密码，不落盘明文。
- 节点管理（增删改、延迟测试）。

### v0.0.1（2026-08-20）

- 首个可运行版本：Tauri 2 + mihomo 内核。
- 软件级智能分流：按 App 决定直连还是走代理。
- 云服务器 SSH 控制台：在软件内连接、查看、操作服务器。

---

## 怎么看历史

```bash
git log --oneline          # 全部提交（一行一条）
git log --oneline --graph  # 带分支图
git tag -l -n1             # 所有版本标记
git show v0.2.2            # 某版本改了什么
git diff v0.2.0 v0.2.2     # 两个版本之间的差异
```

## 备份说明

**git 本身就是备份。** 所有历史版本都在 `.git/` 里，可随时 `git checkout <tag>` 回到任意版本。
不另建 `backup/` 目录存放 .app 包——那是重复且占空间（v0.1.0 旧包已清理）。
发版产物（`.app.tar.gz`、dmg）由 `release.sh` 按需生成，不入库。
